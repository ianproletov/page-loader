plugins:
          # https://github.com/jest-community/eslint-plugin-jest
        - jest
        # https://github.com/babel/eslint-plugin-import
        - import

env:
        node: true
        jest: true

# https://github.com/babel/babel-eslint
parser: 'babel-eslint'

extends: 
        - 'airbnb-base'
        - 'plugin:jest/recommended'

rules:
        no-console: 0